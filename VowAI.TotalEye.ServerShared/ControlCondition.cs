namespace VowAI.TotalEye.ServerShared
{
    public enum ControlCondition
    {
        Contain /* Inspected names contain the given filtering words. */,
        Equal /* Inspected names equal the given filtering words. */,
    }
}
